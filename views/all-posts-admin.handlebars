{{#each posts as |post| }}
<div class="card">
    <div class="card-body">
        <a href="/dashboard/edit/{{post.id}}">
            <h2>{{post.title}}</h2>
            {{formatDate post.date_created}}
        </a>
    </div>
</div>
{{/each}}